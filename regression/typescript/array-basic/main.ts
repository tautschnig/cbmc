// ES2024 sec-array-initializer: Array Initializer
// ES2024 sec-element-access: Element Access (arr[i])
const arr: number[] = [1, 2, 3];
console.assert(arr.length === 3);
console.assert(arr[0] === 1);
console.assert(arr[2] === 3);
