const arr: number[] = [10, 20, 30, 40, 50];
const first: number = arr[0];
const second: number = arr[1];
const third: number = arr[2];
console.assert(first === 10);
console.assert(second === 20);
console.assert(third === 30);
