KNOWNBUG
main.py

^EXIT=0$
^SIGNAL=0$
^VERIFICATION SUCCESSFUL$
--
^warning: ignoring
--
Returning a lambda from a function: make_adder returns a lambda that
captures n. The lambda is stored as a function symbol, but the
closure variable n is not captured (lambda closure capture is not
yet implemented — only named nested functions are supported).
Affects ~5 ESBMC tests.
