CORE
main.py
--unwind 6
^EXIT=10$
^SIGNAL=0$
^VERIFICATION FAILED$
--
^warning: ignoring
--
A Union (int|str) attribute mutated to str within the SAME binary expression's
operands (e.gm() mutates e.x, then e.x is read) must raise TypeError. The
arithmetic tag obligation (python_converter_ops.cpp) already catches the
straightforward forms (str+1 as param/local/attr, and after a separate-statement
mutation); the residual is narrowly the same-expression case -- an evaluation-
order x union-retag interaction, not a missing obligation. PLR-correct outcome:
VERIFICATION FAILED.
