CORE
main.py
--unwind 5

^EXIT=10$
^SIGNAL=0$
^VERIFICATION FAILED$
--
^warning: ignoring
--
Optional[int] now lowers to the python_value tagged union (NONE tag
distinguishable from a real int), so calling f(None) and computing
x + 1 raises TypeError via the numeric-binop tag obligation. Previously
Optional[T] collapsed to T (None erased) and the TypeError was missed
(differential unsoundness witness 8). Now detected; promoted to CORE.
