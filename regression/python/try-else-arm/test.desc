CORE
main.py
--unwind 6 --no-unwinding-assertions
^EXIT=0$
^SIGNAL=0$
^VERIFICATION SUCCESSFUL$
--
--
try/else as the else arm of the handler dispatch: else skipped when the
try body raised (even though the handler clears the flag), a raise inside
else propagates past this try's handlers, try/else/finally ordering.
