KNOWNBUG
main.py
--unwind 6
^VERIFICATION FAILED$
--
--
Desired: the annotated extraction `r: list = c[0]` must be guarded like the
unannotated `r = c[0]` (which correctly reports a model-bound / fails the stale
assert). Currently the AnnAssign path skips extracted_container_alias recording,
so cbmc proves the stale c[0]==[1,2] (false proof). Fix: record the alias on the
annotated-assignment path too. Promote to CORE when fixed.
