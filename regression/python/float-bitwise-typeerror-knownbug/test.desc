KNOWNBUG
main.py
--unwind 3
^VERIFICATION FAILED$
--
--
Desired: a bitwise/shift/invert op (& | ^ << >> ~) with a float operand raises
TypeError (the value never reaches the assert). cbmc currently models it (false
proof). Covers 1.0&2, 1.0<<2, ~1.0. Promote to CORE when a float-operand tag
obligation for the integral operators is added.
