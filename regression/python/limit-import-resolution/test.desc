KNOWNBUG
main.py

^EXIT=0$
^SIGNAL=0$
^VERIFICATION SUCCESSFUL$
--
--
Counter returns nondet (no body). The assertion c[1] == 2 fails
because the nondet dict doesn't track element counts.
With --python-no-body-check, verification succeeds (no crash).
