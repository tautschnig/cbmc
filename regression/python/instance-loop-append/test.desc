CORE
main.py
--unwind 8 --no-unwinding-assertions
^EXIT=0$
^SIGNAL=0$
^VERIFICATION SUCCESSFUL$
--
--
instances appended in a loop keep distinct identities: boxing
heap-allocates a fresh record per instance (per unwound iteration)
instead of reusing a per-syntactic-site static temp.
